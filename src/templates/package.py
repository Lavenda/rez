name = "{PACKAGE_NAME}"

version = "{VERSION}"

authors = [
    "{AUTHORS}"
]

description = \
    """
    {DESCRIPTION}
    """

tools = [
]

variants=[
]

requires = [
]

uuid = "{UUID}"

def commands():
    {COMMANDS}
